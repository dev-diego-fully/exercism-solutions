#if !defined(HEXADECIMAL_H)
#define HEXADECIMAL_H

#include <string>
#include <vector>
#include <cctype>
#include <cmath>

namespace hexadecimal {

int convert(std::string hex_code);

}  // namespace hexadecimal

#endif // HEXADECIMAL_H