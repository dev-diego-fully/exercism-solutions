#include <regex>
#include <string>
#include <vector>

#if !defined(ACRONYM_H)
#define ACRONYM_H

namespace acronym {

std::string acronym(const std::string &phrase);

} // namespace acronym

#endif // ACRONYM_H